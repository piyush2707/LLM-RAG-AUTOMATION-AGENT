# --- LLM API Key Configuration ---

# Required for running both ingestion.py and the API (query_rag_agent).
# Get your key from the OpenAI website.
OPENAI_API_KEY=your_openai_api_key_here

# --- Docker/CI/CD Configuration (Used by ci_cd.yml) ---

# Your Docker Hub username for pushing the container image.
DOCKER_USERNAME=your_docker_hub_username

# Your Docker Hub Personal Access Token (PAT), not your password.
DOCKER_TOKEN=your_docker_hub_personal_access_token

# --- Optional Configurations ---

# You can add other optional configurations here, like custom model names or database paths.
# LLM_MODEL_NAME=gpt-3.5-turbo
# CHROMA_DB_PATH=vector_store
